version: 0.1
frontend:
  phases:
    build:
      commands:
        - rm -rf node_modules
        - npm run build
  artifacts:
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
