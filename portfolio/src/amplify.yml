version: 0.1
frontend:
  phases:
    build:
      commands:
        - npm react-scripts build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
